{{ header }}
{{ menu }}

  <div class="qu-sec py-2 ">
  <div class="container container-custom">
    <div class="row">
<div class="col-md-12 col-sm-12">
  <nav class="breadcumb-navigation">
    <ol class="breadcrumb m-0">
      {# <li class="breadcrumb-item"><a href="#"><i class="fas fa-home"></i></a></li> #}
      {% for breadcrumb in breadcrumbs %}
      <li class="breadcrumb-item active" aria-current="page"><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
      {% endfor %}
    </ol>
  </nav>   
</div>
</div>
    </div>
</div>
  {% if success %}
  <div class="alert alert-success alert-dismissible"><i class="fa fa-check-circle"></i> {{ success }}</div>
  {% endif %}
  {% if error_warning %}
  <div class="alert alert-danger alert-dismissible"><i class="fa fa-exclamation-circle"></i> {{ error_warning }}</div>
  {% endif %}
    <section class="login-reg-sec">
      <div class="row">
        <div class="col-md-7 col-sm-6 col-xs-6 width-30">
          <div class="login-side">
               <img src="catalog/view/javascript/assets/image/popupimg/slider1.png" class="img-responsive">
          </div>
        </div>
        <div class="col-md-5 col-sm-6 col-xs-6 width-70">
        <!--- sign in form-->
          <div class="signin-form-popup signupform1 selected">
            <div class="from-header">
              <h6> Welcome to  <br> <span> UrbanWood</span> </h6>
              <p>Glad to See You!</p>
            </div>
            <div class="popup-signform">
              <!-- class="loginform" -->
              <form method="post" action="?route=account/login">
                <div class="row">
                  <div class="col-md-12">
                    <input type="email" name="email" class="form-control" id="exampleInputEmail1" placeholder="Email Address" required="">
                  </div>
                  <div class="col-md-12 ">
                    <input type="password" name="password" class="form-control password" autocomplete="current-password" required="" id="id_password" placeholder="Password">
                    <i class="far fa-eye-slash" id="togglePassword"></i>
                  </div>
                  <div class="d-block">
                    <button type="submit" class="login-btn-popup">Login</button>
                    <a href="?route=account/forgotten" class="forget-pass text-center">Forget password?</a>
                    {# <div class="d-flex justify-content-center align-items-center my-3">
                      <span class="line"></span><p class="or-way-tolog">OR Login with</p> <span class="line"></span>
                    </div> #}
                    {# <div class="d-flex justify-content-center align-items-center my-3">
                      <img src="./assets/image/icon/g-icon.png" alt="" width="40" class="mx-1">
                      <img src="./assets/image/icon/f-icon.png" alt="" width="40" class="mx-1">
                    </div> #}
                    <p class="text-center dont-accont">Dont Have an Account ? <a href="?route=account/register" >Sign Up Now</a></p>
                  </div>
                </div>
              </form>
            </div>
          </div>
        </div>
      </div>
    </section>

    <style>
  :root{
    --primary:#d70909;      /* Urbanwood red */
    --muted:#555;
    --card-bg:#fff;
    --page-bg:#f6f6f6;
    --radius:14px;
    --shadow: 0 8px 24px rgba(0,0,0,.04);
  }

  html,body{height:100%; margin:0; font-family:Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;}
  body{background:var(--page-bg); display:flex; align-items:center; justify-content:center; padding:32px;}

  /* Container: 40% width on desktop, responsive */
  .auth-wrap {
    width: min(760px, 90%); /* caps width on small screens */
    display:flex;
    align-items:center;
    justify-content:center;
  }

  .auth-card{
    width:40vw;                /* ~40% of viewport width on desktops */
    max-width:420px;          /* don't blow up on very large monitors */
    min-width:300px;
    background:var(--card-bg);
    border-radius:var(--radius);
    box-shadow:var(--shadow);
    padding:28px;
    box-sizing:border-box;
    border:1px solid #eee;
  }

  /* Mobile fallback: make it wider on small screens */
  @media (max-width:900px){
    .auth-card{ width:92vw; max-width:520px; padding:20px; }
  }

  .brand {
    display:flex; align-items:center; gap:12px; margin-bottom:18px;
  }
  .brand .logo {
    width:44px; height:44px; background:linear-gradient(135deg,#fff, #f5f5f5); border-radius:10px; display:flex; align-items:center; justify-content:center; box-shadow: 0 4px 12px rgba(0,0,0,.04);
    font-weight:800; color:var(--primary);
  }
  .brand h1 { font-size:20px; margin:0; color:#222; letter-spacing:0.1px; }
  .brand p { margin:0; color:var(--muted); font-size:13px; }

  .title {
    font-weight:700; font-size:18px; margin:6px 0 16px;
  }

  /* google button */
  .btn-google {
    display:inline-flex;
    align-items:center;
    gap:12px;
    width:100%;
    padding:12px 14px;
    border-radius:999px;
    border:1px solid #ddd;
    background:#fff;
    box-shadow: none;
    font-weight:600;
    cursor:pointer;
    user-select:none;
    text-decoration:none;
    color:#202124;
    transition:transform .06s ease, box-shadow .08s ease, opacity .08s ease;
  }
  .btn-google:active{ transform:translateY(1px); }
  .btn-google:focus{ outline:3px solid rgba(215,9,9,.12); outline-offset:2px; }
  .btn-google.disabled, .btn-google[aria-disabled="true"]{ opacity:.6; pointer-events:none; }

  .btn-google .g-icon{
    width:20px; height:20px; display:inline-block;
  }

  .divider { display:flex; align-items:center; gap:12px; margin:18px 0; color:#777; font-size:13px; }
  .divider::before, .divider::after { content:""; height:1px; background:#eee; flex:1; border-radius:2px; }

  form.form-email { margin-top:6px; }
  .form-row { display:flex; flex-direction:column; gap:8px; margin-bottom:12px; }
  input[type="email"]{
    padding:10px 12px; border-radius:10px; border:1px solid #ddd; outline: none; font-size:14px;
  }
  input[type="email"]:focus{ box-shadow:0 0 0 4px rgba(215,9,9,.06); border-color:var(--primary); }

  .btn-primary {
    display:inline-flex; align-items:center; justify-content:center;
    gap:8px;
    padding:10px 14px;
    width:100%;
    background:var(--primary);
    color:#fff; border:none; border-radius:999px;
    font-weight:700; cursor:pointer;
  }
  .btn-secondary {
    display:inline-flex; align-items:center; justify-content:center;
    gap:8px;
    padding:10px 14px;
    width:100%;
    background:#fff; color:#333; border:1px solid #ddd; border-radius:999px; font-weight:700; cursor:pointer;
  }

  .minor { font-size:13px; color:#666; margin-top:10px; }
  .link { color:var(--primary); text-decoration:none; font-weight:600; }

  /* small spinner for button */
  .spinner {
    width:16px; height:16px; border-radius:50%; border:2px solid rgba(0,0,0,.08); border-top-color:rgba(0,0,0,.55); animation:spin .9s linear infinite;
  }
  @keyframes spin { to { transform:rotate(360deg); } }

  /* center small note at bottom */
  .auth-footer { font-size:12px; color:#999; text-align:center; margin-top:14px; }
</style>
<div class="auth-wrap">
  <div class="auth-card" role="region" aria-label="Sign in box">
    <div class="brand">
      <div class="logo">UW</div>
      <div>
        <h1>Urbanwood</h1>
        <p>Sign in to manage orders & addresses</p>
      </div>
    </div>

    <div class="title">Sign in to your account</div>

    <!-- Google OAuth button (replace href with your real OAuth URL) -->
    <a id="googleBtn" class="btn-google" href="YOUR_GOOGLE_OAUTH_URL" role="button" aria-label="Sign in with Google">
      <!-- Google SVG Mark -->
      <span class="g-icon" aria-hidden="true">
        <svg width="20" height="20" viewBox="0 0 48 48" xmlns="http://www.w3.org/2000/svg" focusable="false">
          <path fill="#EA4335" d="M24 9.5c3.7 0 6.4 1.6 7.9 2.9l5.8-5.6C34.6 4.1 29.7 2 24 2 14.7 2 6.9 6.9 3.4 14.2l6.7 5.2C11.4 15 17 9.5 24 9.5z"/>
          <path fill="#34A853" d="M46.5 24.5c0-1.6-.2-2.8-.5-4H24v7.6h12.6c-.6 3.2-3 7.4-8.6 9.8l6.6 5.1C42.6 38.2 46.5 31.9 46.5 24.5z"/>
          <path fill="#4A90E2" d="M9.3 28.9A14.9 14.9 0 0 1 8 24c0-1.9.4-3.6 1.1-5.2L2.4 13.6C.9 16.7 0 20.2 0 24c0 3.8.9 7.3 2.6 10.4l6.7-5.5z"/>
          <path fill="#FBBC05" d="M24 46c6.3 0 11.5-2.1 15.4-5.6l-7.3-5.6c-2.2 1.6-5 2.6-8.1 2.6-7 0-12.6-5.5-14.5-10.5L4.6 33.7C7.8 39 14.6 46 24 46z"/>
        </svg>
      </span>

      <span class="label">Sign in with Google</span>
      <span class="spinner" id="googleSpinner" style="display:none;"></span>
    </a>

    <div class="divider">or</div>

    <!-- Alternate email form (example) -->
    <form class="form-email" action="#" method="post" onsubmit="onEmailSignIn(event)">
      <div class="form-row">
        <label for="email" style="font-size:13px;color:#444">Email address</label>
        <input id="email" name="email" type="email" required placeholder="you@example.com" autocomplete="email" />
      </div>

      <div style="display:grid; gap:10px;">
        <button type="submit" class="btn-primary">Continue with Email</button>
        <button type="button" class="btn-secondary" onclick="window.location='YOUR_GOOGLE_OAUTH_URL'">
          <i class="fa fa-google" style="margin-right:8px"></i> Sign in with Google
        </button>
      </div>
    </form>

    <div class="minor">Don't have an account? <a class="link" href="#">Create one</a></div>

    <div class="auth-footer">By continuing you agree to our <a href="#" class="link">Terms</a> and <a href="#" class="link">Privacy Policy</a></div>
  </div>
</div>
{{footer}}